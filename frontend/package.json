{
  "name": "fantasy-tracker-frontend",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "echo \"No frontend yet\" && exit 0",
    "build": "echo \"No frontend build configured\" && exit 0",
    "test": "echo \"No tests\" && exit 0"
  },
  "devDependencies": {}
}
