# Frontend (placeholder)

Este directorio contiene el esqueleto del frontend. Actualmente no hay una aplicación Angular creada.

Se incluye package.json mínimo para que el workflow de CI detecte el proyecto y utilice Node 24.

Cuando crees el proyecto Angular, sustituye este contenido por la app generada por `ng new frontend`.
